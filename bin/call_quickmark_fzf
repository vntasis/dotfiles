#!/bin/bash

set -eu

quickmark_tmp='/home/vntasis/.quickmark'

alacritty --class 'fzf_menu,Quickmarks' -e 'quickmark_fzf' && \
    cat $quickmark_tmp | xclip -selection 'clipboard' && \
    rm $quickmark_tmp
